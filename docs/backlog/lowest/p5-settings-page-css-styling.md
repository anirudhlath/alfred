# P5: Settings Page CSS Card Styling

## Summary
Integration cards render functionally but CSS classes aren't applying properly — cards appear unstyled.

## Context
Likely a specificity or class name mismatch issue in `web/style.css` + `web/settings.js`.

## Acceptance Criteria
- Integration cards styled correctly
- CSS specificity issues resolved
- Visual consistency with rest of PWA

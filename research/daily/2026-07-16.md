# Daily Note — 2026-07-16

## Live E2E run (post PR #22/#23)

First full-system run with background startup warmup active. Measured from
runner logs (M4 Max, cold vs. warm Ollama cache):

| Component | Process | Load time |
|---|---|---|
| Ollama model (gpt-oss:20b) | reflex | 15.2s cold / 0.2s warm |
| Whisper STT (large-v3-turbo) | channels | 3.7s / 2.2s |
| Piper TTS (alan-medium) | channels | 4.3s / 2.6s |
| EmbeddingGemma-300M | memory-ingestor | 7.2s / 5.2s |
| EmbeddingGemma-300M | conscious | ~7.6s |
| Redis vector index / SQLite cold store | both | ≤0.1s |

All services served immediately; models loaded concurrently in background.

## Sensor trigger e2e (first ever successful run of this path)

MQTT `home/state_changed` (TV on) → bridge → `alfred:home:state_changed` →
trigger engine fired one-shot sensor trigger → TriggerFired → reflex
notification → delivered via Signal + APNs. End-to-end delivery within ~1s of
publish (log timestamps 23:50:09 → 23:50:10). Prior to PR #22 this path never
fired (wrong stream subscription).

## Anomalies found & fixed during the run (PR #23)

- `UNIQUE constraint failed: schema_version.version` — non-idempotent v1 seed
  on shared episodic_cold.db under concurrent warmup.
- `database is locked` — schema ensure took write locks on every startup;
  now read-only fast path + busy_timeout=10s.

Token CSVs gained rows from the live trigger-fire SLM call; no cost anomalies.
